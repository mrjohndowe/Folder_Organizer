<Window x:Class="FolderOrganizer.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Folder Organizer Settings"
        Width="900"
        Height="600"
        MinWidth="700"
        MinHeight="450"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0"
                    Margin="0,0,0,16">

            <TextBlock Text="Settings"
                       FontSize="26"
                       FontWeight="Bold"/>

            <TextBlock Text="Custom rules are checked before built-in organization rules."
                       Margin="0,4,0,0"
                       Opacity="0.65"/>

        </StackPanel>

        <!-- Custom Rules Header -->
        <Grid Grid.Row="1"
              Margin="0,0,0,8">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Custom Folder Rules"
                       FontSize="18"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                x:Name="AddRuleButton"
                Content="+ Add Custom Rule"
                Width="145"
                Height="34"
                Click="AddRuleButton_Click"/>
        </Grid>

        <!-- Rules -->
        <DataGrid Grid.Row="2"
                  x:Name="RulesDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  CellEditEnding="RulesDataGrid_CellEditEnding"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal">

            <DataGrid.Columns>

                <DataGridTextColumn
                    Header="Priority"
                    Binding="{Binding Priority}"
                    IsReadOnly="True"
                    Width="80"/>

                <DataGridCheckBoxColumn
                    Header="Enabled"
                    Binding="{Binding IsEnabled}"
                    Width="75"/>

                 <DataGridTextColumn
                    Header="Folder"
                    Binding="{Binding FolderName}"
                    Width="2*"/>

                 <DataGridTextColumn
                    Header="Extensions"
                    Binding="{Binding Extensions}"
                    Width="3*"/>
            </DataGrid.Columns>

        </DataGrid>

        <!-- Bottom -->
        <Grid Grid.Row="3"
              Margin="0,12,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusTextBlock"
                       Text="Ready."
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="Close"
                    Width="100"
                    Height="34"
                    Click="CloseButton_Click"/>

        </Grid>

    </Grid>
</Window>